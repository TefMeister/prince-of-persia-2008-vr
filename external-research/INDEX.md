# Research index

**Last `/gr` pass: 2026-09-11 (estate sweep, evening, home PC) — CHECK-IN.** Inbox empty; board unchanged and all `[FLAT]` (the walk-and-turn follow-camera test). Two targeted searches on the one static question the 2026-09-08 pass left open — a field list for `PopFreeRoamingCamera` — returned **no schema** (AnvilToolkit's public schema-exporter list still names no camera type), but one small corroboration is filed as a 🆕 topic: an AC2 Cheat Engine mod tunes "the default Ground Cam" by **FOV, distance and position offset**, so "Ground Cam" is a family-wide name for the ordinary follow camera (our repoint target) and those three are the fields the deferred decode should expect; its author's "movement drifting" note is a hazard for any head-offset step. Everything `[reported]`; Nexus pages 403 to automated fetch, so re-read by a human before leaning on it.

_Previous: **Last `/gr` pass: 2026-09-08 (estate sweep) — CHECK-IN, inbox drain.** **Inbox drained: 2 files, a correction pair.** The modding lane ran the CRC-32 type-ID test this topic asked for and it **paid** — the scheme holds on this 2008 build, and the sub-object tagging idea produced the discriminator behind the whole camera result (`PadButtonReader`/`PadAxisReader` in `PopMarketingCamera` only, none of the other **879**). Two structural priors are refuted for this build (`*Holder` is a single reference, not a list; follow-vs-free is not a target reference) while the structural *shape* they came from was right. Status tags flipped: the CRC-32 topic 🆕→✅, the gamepad-confounders topic 🆕→👀 (still unrun, and the `[USER]` row it served has since been deleted). ⚠️ The pair arrived with a `Supersedes:` header because the author caught a **sample size written from memory** minutes after pushing — 481/`n=484` where the committed census says **879/`n=882`** — so both files were read before either was folded in, and the corrected figure is what went into this index. Nothing was researched on the web this pass. **Asked of a future pass:** a resolved field list for `PopFreeRoamingCamera` (the AnvilToolkit AC1 schema-exporter route), against 3,608 undecoded bytes per block._

_Previous: **Last `/gr` pass: 2026-09-07 (estate sweep, FOURTH pass today) — FULL** (board `OPEN` block re-read after the camera takeover was confirmed live, INDEX, all three forge/camera topics; one delegated schema hunt across the Anvil modding ecosystem)**.** Inbox empty. **The board moved again: the `(309,309,309)` edit WORKS** — a free-flying detached debug camera in normal gameplay, from pure data, no code patch — and the new ⭐⭐ `[PD]` row (which rule is winning, and can it be locked to the Prince?) is the critical path. **Two topics filed today for this project; this is the second.** ❌ **No public Anvil camera schema exists**, on a well-controlled negative. ⭐⭐ **But three testable things came back:** five **CRC-32 type IDs** to check against records we already parse (one match makes the scheme ground truth and unlocks brute-forcing *field* names, since Anvil stores them only as 32-bit hashes); a **structural prior** saying follow-vs-free is an **object reference** rather than a boolean, that **child order is the priority**, and that **`*Holder` means "list container"**; and a **shipped AC4 data-only `GraphRuleBook` camera mod** as precedent for exactly this technique. `CGST_`/state 309 is answerable **internally** from our own 876 state lists. Pointer filed to `engine-research/inbox/`. ⚠️ Everything cross-generation is `[hypothesis]` — 2008 vs 2013/2014/2019 — and two era-appropriate references (TBotR wiki, XeNTaX) are **dead links rather than absent sources**._

_Previous: **Last `/gr` pass: 2026-09-07 (estate sweep, THIRD pass today) — FULL** (board `OPEN` block re-read after the same day's six-commit input breakthrough, INDEX, both forge topics, the control profile; two targeted searches)**.** Inbox empty. **The board moved hard between passes: input is SOLVED here** — injection inside `GetDeviceState`, after the real call, live-verified. **One new topic, aimed at the one row that costs the USER's time.** 🚨 Steam Input is reported to **crash this game on launch when controllers are connected** (fix: *Steam Input Per Game Setting* → `Forced Off`), and the Steam Controller specifically is reported incompatible — so **the likeliest outcome of the `[USER]` gamepad row as written is the game not launching**, which reads as "the pad didn't help" and would be a false negative on the leading hypothesis. ✅ Balanced by a community report of **A advancing the splash screen**, which is exactly what the row predicts. ⚠️ Plus a hazard for *any* input work here (the Steam overlay makes the game ignore all input until restart) and a ViGEm caveat if a virtual pad is ever substituted. ⚖️ One lead checked and deliberately **not** acted on (V-Sync hiding the prompt) because it describes what the game *draws*, not what it *reads*. **Every finding is `[reported]` and our own measurements outrank all of them** — what changes is the conditions the test runs under, not what it means. Pointer filed to `engine-research/inbox/`._

_Previous: **Last `/gr` pass: 2026-09-07 (estate sweep, second pass today) — CHECK-IN, targeted** (board `OPEN` block + INDEX + the two forge topics; two searches and one verifying fetch on the `[PD]` row)**.** Inbox empty. **One small new topic, honestly scoped as corroboration rather than a decode.** The `MAGMA` structure is still undocumented publicly — same negative as 2026-09-05, from searches that demonstrably reached the right corpus. What is new: **"Game Bootstrap" is a named, routinely-edited block across the Anvil family** — a public AC4 guide has readers open and replace `DataPC.forge → 51-Game Bootstrap Settings.data` — so the block class is a persistent engine concept, the sibling community edits it by **whole-block export → edit → reimport** (the exact shape of our validated repacker, which is the `[USER]` row), and its AC-side contents are **references and IDs** rather than opaque binary `[hypothesis]`. ⚠️ Does **not** decode MAGMA, the block numbering does not transfer (51 vs 419), PoP remains excluded from all the tooling, and AC4 is five years downstream. Also noted: `.magma_localization` files exist in the AC series, so `MAGMA` is a **family-wide container tag**, which makes the absence of any public description more surprising, not less._

_Previous: **Last `/gr` pass: 2026-09-07 (estate sweep) — CHECK-IN** (board `OPEN` block + INDEX; nothing searched)**.** Inbox empty. **Nothing new.** Board unchanged since 2026-09-05 and its top row is `[USER]` — a deploy-or-decline decision on the validated repacker. The `[PD]` `MagmaCommon_MGB` row had ~4 targeted searches on 2026-09-05; nothing has changed publicly in two days._

_Previous: **Last `/gr` pass: 2026-09-05 (estate sweep) — CHECK-IN, targeted** (board OPEN block + INDEX; ~4 searches on the `[PD]` row's file format)**.** Inbox empty. **One useful negative, recorded so nobody re-runs it.** Today's `/pd` disproved the `ActionBlock` route (all 2,464 blocks searched for all 19 menu-handler ids, zero hits), leaving the `[PD]` row to decode **`MagmaCommon_MGB`** (`MAGMA` magic at +0x30) or to read `DebugMenuHandler_m`'s class out of the exe. **No public documentation of the MAGMA/MGB inner format was found** `[checked 2026-09-05]` — searches across the web and GitHub return the outer `.forge` container, which this project already parses and repacks successfully, and nothing at all about the format inside it. **Treat the MAGMA decode as ours to do**; this is not a case of finding the right tool. One same-lineage resource worth knowing if the *forge* half ever matters again: `dataterminals/grb-modding-knowledgebase` documents Ubisoft **Anvil/Scimitar** `.forge` for Ghost Recon Breakpoint — but that is a 2019 title against this 2008 one, the *first* Scimitar game, so expect the container to have moved a long way. The `[USER]` repacker-deployment row is unchanged and is a decision, not a question._

_Previous: **Last `/gr` pass: 2026-09-04 (estate sweep) — CHECK-IN** (board OPEN block + INDEX)**.** Inbox empty. **Nothing new, and not searched** — the repacker is built and validated and its deployment is `[USER]`; the console probe is `[FLAT]`. The 2026-09-03 FULL pass already reframed the debug-menu entry onto the data route._
_Previous: **Last `/gr` pass: 2026-09-03 (estate sweep) — FULL** (inbox drained, INDEX and the `.forge` topic read in full, board read)**.** Drained the modding side's verdict — the CRC32 hash needle is `[disproved]`, but the destination was right and the debug FPS camera IS authored in shipped PC data. Answered the question the drop closed with: **a debug menu exists in this game's own pause menu** (found by console players in the Epilogue DLC, where the list scrolls past its bounds into a "Menu Debug" screen), so the entry to `CGST_DebugMode` is a **UI/data route, not a console** — which is exactly why `.text` holds no immediate 188/189. Reframes the search onto menu datablocks and the registry **strings** rather than ordinals; all three follow-ups stay `[PD]`. The "no public PC console/debug key" negative now stands at two independent passes._
_Previous: **Last `/gr` pass: 2026-09-02 (estate sweep) — CHECK-IN** (board OPEN block + INDEX; the dossier was not read in full)**.** Inbox empty. **Nothing new.** The `.forge` decoder and the hash search are static work that yesterday's re-ranked topic already scoped; …_

Every research topic gathered for this project, newest first. Each row links to a self-contained
write-up in `topics/`. Status tags:

- 🆕 **new** — found, not yet acted on by the modding side.
- 👀 **reviewed** — a modding session has read it and factored it into a decision, but nothing shipped from it yet.
- ✅ **incorporated** — directly led to a real change (code, a test, a note) in one of the other five repos; linked below.
- ❌ **dead end** — checked out, didn't pan out; kept for the record so it isn't re-investigated from scratch.

| Date | Topic | Status | Summary |
| --- | --- | --- | --- |
| 2026-09-11 | ["Ground Cam" is the family's ordinary follow camera, and its live tunables are FOV, distance and position offset](topics/2026-09-11-ground-cam-is-the-family-follow-camera-and-its-live-tunables-are-fov-distance-offset.md) | 🆕 new | **Small corroboration for the deferred `PopFreeRoamingCamera` field decode, not a schema.** A public **AC2 Cheat Engine mod** edits **FOV, camera distance and camera position offset** of "the default Ground Cam used in game" `[reported 2026-09-11]` — so (1) "Ground Cam" is a **family-wide name for the ordinary follow camera**, one year downstream of this game, an independent point in favour of `Parent Ground Cam` as the repoint target `[hypothesis]` until the `[FLAT]` walk-and-turn test settles it; (2) the three fields the board's deferred decode names are exactly the three a modder found separately writable on that class, which is what makes the 44-block differential-corpus plan worth running; (3) ⚠️ the author reports **movement drifting** when the camera is offset sideways — camera-relative movement showing through — so a first-person head offset here should budget for a movement-direction check, not just a viewpoint check `[hypothesis]`. ❌ Still no public camera schema: AnvilToolkit's schema exporter lists `BuildTable`/`EntityBuilder`/`Cloth`/`Material`-class types and **no camera type**; no page names `FreeRoamingCamera`/`CameraRule`/`GraphRuleBook` for any Anvil title. ⚠️ Nexus pages return 403 to automated fetch — all of it is from search summaries; a same-generation **AC1 "Camera and Controls Fix"** mod exists but its mechanism is unverified. Nothing here is worth doing before the follow-camera test. |
| 2026-09-07 | [Nobody documents Anvil camera datablocks — but five CRC-32 type IDs and a structural prior are testable](topics/2026-09-07c-nobody-documents-anvil-camera-datablocks-but-crc32-type-ids-and-a-structural-prior-are-testable.md) | ✅ incorporated | **✅ VERDICT 2026-09-08 (`/pd`, dev PC, no launch): the CRC-32 type-ID scheme HOLDS on this 2008 build.** All five predicted values occur in this game's data `[verified-numerically 2026-09-08]`, so the cross-generation step this topic named as its own weak link (2014/2019 documentation applied to the *first* Scimitar title) is sound. Two of the five exist only nested inside a `CameraRule` body, which is why our earlier type census never showed them. ⭐ **It produced the discriminator that explains the whole 2026-09-07 camera result:** `PadButtonReader` / `PadAxisReader` occur only inside `PopMarketingCamera` and in **none of the other 879 camera datablocks** `[verified-numerically 2026-09-08, n=882 camera datablocks]` — the debug camera flies because it reads the pad itself. ⚠️ **Two of the three structural priors are WRONG here:** `*Holder` is a single object reference, not a list container `[verified-numerically 2026-09-08]`; and follow-vs-free is not a target-object reference — zero of the 44 `PopFreeRoamingCamera` blocks carry the `PrinceTargetEntity` sub-object that looked exactly like a tracker `[disproved 2026-09-08]`. The topic's *structural* half was right and was the half that mattered: non-following cameras really do live under a different **class**, not behind a boolean. Child-order-as-priority remains **untested** — leave it `[hypothesis]`. ⚠️ **Sample-size correction, same day:** the verdict first reached us as "0 times across the other 481 camera datablocks `[verified-numerically 2026-09-08, n=484]`". That figure was written from memory, not counted; the measured control group is **879 of 882**. The finding is unchanged and is *stronger* than first claimed. 📌 Follow-up worth real time: `GraphRuleBook` is confirmed at `0x4A77CEFE` with 872 blocks, so the shipped AC4 data-only camera mod is operating on a structure we can now identify by hash in our own archive. **Aimed at the ⭐⭐ `[PD]` camera-rule row, now the critical path.** ❌ **No public schema exists** — no `CameraRule` field list, no `CameraExecution`/`CameraHolder` description, no `CGST_` enum, for this game or any Anvil game. Evidenced, not assumed: AnvilToolkit's wiki (the only public datablock-internals reference) **loaded and simply has no camera page**, and GitHub code search returns **0** for `TemporalCameraTransition`, `CGST_DebugMode`, `CGST_DebugModeFPSCamera` and `CR_Debug_1stPerson` against a **capability control of `"CGST_"` returning 38,784 hits** `[verified-numerically 2026-09-07]`. Our decode cannot be shortened by copying a spec — and our edit looks like the **first public data-driven camera change in this game** (every other public PoP camera solution is memory patching; PoP is absent from IGCS's 32 camera folders, and PCGamingWiki records `FOV: false`). ⭐⭐ **But three things are testable.** **(1) Type IDs may be CRC-32 (zlib) of the ASCII type name** — documented from decompiled AnvilToolkit internals, verified on Ghost Recon Breakpoint, and cross-corroborated by `95741049` = `CRC32("Bone")` appearing in both a 2014 and a 2019 title. So: `CameraRule` **1785313731** / `0x6A69B9C3`, `TemporalCameraTransition` **4030866271**, `GraphRuleBook` **1249365758**, `CameraExecution` **446306511**, `CameraHolder` **1022213160** `[verified-numerically as CRC-32; hypothesis that the scheme applies here]`. ✅ **I recomputed all five and checked the scheme against three anchors** — `crc32("BuildTable")`=585940579 and `crc32("Mesh")`=1096652136 reproduce the documented values exactly, and ⭐ `crc32("Bone")`=**`0x95741049`** independently **explains the hex type-ID filename observed in ACExplorer's AC Unity type readers**, which was not a documented value to check against. So the scheme is verified end to end; what remains hypothesis is narrower — whether **this 2008 build** uses it, which is exactly what the test settles. **One match makes the scheme ground truth**, and then the same trick **brute-forces field names from a wordlist** — because Anvil stores field names only as **32-bit hashes** (member records carry a `memberNameHash` and `typeNameHash`; field order is schema order), which is why AnvilToolkit shows unresolved names as `x73B5D0A0`. **(2) A structural prior from reverse-engineered AC Unity layouts:** follow-vs-free is **not a boolean** — what a camera tracks is an **object reference**, and non-following cameras live under a structurally **separate root**, a different node *class*; **arbitration is a tree walk with a boolean condition per node and order in the parent's child array IS the priority** (no priority integer) — so check whether our two rules differ in **child order**; transitions attach to the node, matching our 968 `TemporalCameraTransition` records; and ⭐ **`*Holder` is an Anvil naming idiom for "container of a list"**, not "thing that holds the camera in space" — so expect `CameraHolder` to be a reference/list container, not a mode flag. **(3) `GraphRuleBook` has a shipped precedent:** an **AC4 data-only camera mod** edits two `GraphRuleBook` files out of `Game Bootstrap Settings.data` (`Fight Book` for combat camera distance, `Run_Sprint Book` to remove shake) using unpack→drop→repack — our own workflow, and it ties back to this lane's earlier "Game Bootstrap is a named editable block" finding. **`CGST_`/state 309 is an internal question**: nothing public names them, but our **876 `CameraRule` state lists are a frequency corpus** the default state falls out of numerically. **One route could halve the work** — AnvilToolkit's generic **AC1–Syndicate schema exporter**, AC1 being our engine generation — ⚠️ but ATK does not support PoP, and its changelog was **summarised rather than read**, so "no camera exporter" is not a strong negative. ⚠️ **Every cross-generation claim is `[hypothesis]`:** our target is 2008/LZO2A; the reflection headers are AC Unity 2014, the CRC-32 documentation is GRB 2019/Oodle/forge v27, the mod is AC4 2013. ⚠️ **Archival gaps, not negatives:** `wiki.tbotr.net` is dead and XeNTaX is gone, and ACExplorer's wiki *defers to TBotR* for format depth — the era-appropriate reference is **lost**, so an Internet Archive attempt is the obvious follow-up. AC1's 1.0/3.3 MB name lists were not examined and are a good untested lead. |
| 2026-09-07 | [Two documented confounders would sabotage the `[USER]` gamepad test](topics/2026-09-07b-two-documented-confounders-would-sabotage-the-gamepad-test.md) | 👀 reviewed | **👀 REVIEWED 2026-09-08 (`/pd`): standing, and still unused.** Both confounders are folded into the dossier and cited in the note for the XInput proxy work, but **no gamepad test has been run**, so neither has been observed first-hand — they stay `[reported]`, and our own measurements would outrank them. ⚠️ **The `[USER]` gamepad row this topic was aimed at no longer exists**; it was deleted on 2026-09-07 under the standing "every install stays a dev build" rule. The advice is not wasted — it applies to any future pad test on this game — but nothing is currently waiting on it. **Aimed at the one row that costs the USER's time**, before they are asked to do it. 🚨 **Steam Input is reported to make this game crash on launch when controllers are connected**, with the documented fix being *Steam Input Per Game Setting* → **`Forced Off`**; and **"the game will fail to launch while the Steam Controller is connected"** `[reported 2026-09-07]`. This install is Steam app 19980, so it applies. **The most likely bad outcome of the row as written is the game not launching** — indistinguishable at a glance from "the pad didn't help", i.e. a false negative on the estate's leading input hypothesis caused by a platform setting. **Prerequisite to add: force Steam Input off, and use a plain XInput pad, not a Steam Controller.** ✅ **And one report supports the hypothesis:** a community account of **using A to advance from the splash screen** while being unable to navigate menus or play without keyboard and mouse — second-hand, but a direct instance of what the row predicts. ⚠️ **Second confounder for ANY input work here:** opening the Steam overlay is reported to make the game **ignore all input, keyboard or controller, until restart** — silent and total. ⚖️ **One thing checked and deliberately NOT acted on:** V-Sync is reported to stop the "Press any button" prompt **displaying**. That does not explain our symptom — we measured that the title screen **ignores injected keyboard and does not poll DirectInput at all**, which is about what the game *reads*, not what it *draws*; a missing prompt on a screen that still accepts input would not give our result. Free to eliminate anyway, not a redirection. **Our first-party measurements outrank every `[reported]` item here and stand unchanged.** ⚠️ If a **virtual** pad is ever substituted for the physical one: this machine carries a **broken ViGEm bus instance** (`ROOT\SYSTEM\0004`, Error, pre-existing) found by a sibling project `[measured 2026-09-07]`, so a ViGEm stand-in would fail for an environment reason and look like a fact about the game. The project's own `xinput1_3` DLL proxy is a different mechanism and unaffected. The `[PD]` virtual-pad row is untouched — no public source describes this game's XInput polling, and its own plan (log **every** `XInputGetState` entry to separate *never called* from *apply declined*) remains the cheapest discriminator. |
| 2026-09-07 | ["Game Bootstrap" is a named, editable block across the Anvil family](topics/2026-09-07-game-bootstrap-is-a-named-editable-block-across-the-anvil-family.md) | 🆕 new | **Corroboration for the `[USER]` repacker decision — explicitly NOT a MAGMA decode.** The `[PD]` row targets `DataPC.forge → Game Bootstrap` block 419 with `MAGMA` magic at +0x30, and the structure is **still undocumented publicly** (same negative as 2026-09-05, from searches that reached the right corpus — the engine-identity wikis, the ZenHAX Anvil thread, ACExplorer's Forge File System page, the ModDB tool listings). What is new: a public AC4 guide instructs readers to open and replace **`DataPC.forge → 51-Game Bootstrap Settings.data`** `[reported 2026-09-07]`, so (1) **"Game Bootstrap" is a persistent, named engine concept across the Anvil/Scimitar line**, present by name in a 2013 title as in this 2008 one; (2) the sibling community edits it by **whole-block export → edit → reimport**, which is exactly the shape of our already-validated `forge_write.py` — i.e. the `[USER]` row's route is the one they actually use; and (3) its AC-side contents are **references and IDs** (ship identifiers, a progression-manager block reference), faint evidence a Game Bootstrap block is a reference table, the shape `P_PauseMenuDebug` entries would need to be enumerable `[hypothesis]`. ⚠️ **Read the limits:** block numbering does not transfer (51 vs 419); **PoP is still absent from every tool's support list**, as this lane established 2026-09-01; AC4 is five years and several engine revisions downstream; and the guide is deprecated and was removed from Steam Community, cited only for the block name and workflow. ⚠️ **Method:** the name first surfaced in a search summary and was then **verified by an open-ended fetch** ("list every datablock this guide tells the reader to replace"), not by asking whether the page contained the string — this lane's own rule from earlier today. Also: `.magma_localization` files exist in the AC series, so `MAGMA` is a **family-wide container tag**, not PoP-specific. |
| 2026-09-03 | [The debug menu is a pause-menu entry, not a console](topics/2026-09-03-the-debug-menu-is-a-pause-menu-entry-not-a-console.md) | 🆕 new | **⭐ Critical path — reframes the one open question.** Console players reached a **"Menu Debug"** screen in this game's own pause menu (Epilogue DLC, where the list scrolls past its bounds) with corruption toggles on it. So the way into `CGST_DebugMode` is a **menu/data route, not a console** — and a name-driven UI path is precisely what leaves **no 32-bit immediate 188/189 in `.text`**, so the modding side's static negative is not evidence of absence. Caveats stated: the report is console-only (PC never got the Epilogue), though PC data independently ships the camera half. Redirects the search onto **menu datablocks** and the **registry strings** `CGST_DebugMode`/`CGST_DebugModeFPSCamera` rather than their ordinals or hashes, plus `GraphRuleBook`'s `Ingame_FreeCam` — all static, all `[PD]`. Still negative: no public PC console, debug key or `/console` switch, now at two passes. |
| 2026-09-01 | [`.forge` tooling reaches the datablocks, but nobody has read a graph](topics/2026-09-01-forge-tooling-reaches-datablocks-but-nobody-has-read-a-graph.md) | ✅ incorporated | **Acted on 2026-09-02 — destination right, needle wrong.** Rather than downloading Elika, the modding side wrote a `.forge` reader from scratch: all 33,401 datafiles parse with zero cross-check failures, payloads are chunked LZO2A. The topic's mandatory positive control then **`[disproved]` the CRC32 hash-needle method** — every hit for `CGST_Idle`/`Walk`/`Ground` fell inside audio blocks at identical offsets, chance bytes; states are referenced by **ordinal**, not hash. Without the control this would have read as "the debug camera is absent". The destination was right anyway: `DataPC.forge` ships `CR_Debug_1stPerson` on states 188/189, `CR_Debug_GhostCam`, two transitions, `CAM FPS` and `CAM Ghost POP`, and the camera system is confirmed fully data-driven (876 `CameraRule`, 968 `TemporalCameraTransition`, 2,631 `GraphRuleBook`). Follow-up in the 2026-09-03 topic above. **⭐ Original framing:** Answers both questions from the modding side with honest negatives — public `.forge` documentation covers the container only (no type IDs, no datablock schema; header magic is the literal `scimitar`), Elika reaches datablocks but interprets only textures/meshes/localization, and **AnvilToolkit's support list omits Prince of Persia entirely**. Then the way through: `CGST_DebugModeFPSCamera`'s **hash `0xA80488AB`** is a schema-free needle to search extracted datablocks with — with a mandatory positive control, since a bad extraction looks exactly like a real negative. |
| 2026-08-25 | [Middleware confirmed: DARE audio + Havok physics](topics/2026-08-25-middleware-confirmed-dare-audio-havok-physics.md) | ✅ incorporated | "Dare" resolved as Ubisoft's own cross-project audio system (also used in Splinter Cell, not Anvil-specific); Havok physics confirmed. Fills two open §2 blanks. |
| 2026-08-25 | [Cheat table camera check + debug-menu gap](topics/2026-08-25-cheat-table-camera-check-and-debug-menu-gap.md) | ✅ incorporated | Camera direction is memory-readable (per a Cheat Engine table) but no dedicated FOV/free-cam tool exists, unlike Mad Max. No public debug-menu/console unlock found either — both honest negative results, sets expectations for from-scratch work. |
| 2026-08-25 | [HelixMod 3D Vision fix — major camera prior art](topics/2026-08-25-helixmod-3d-vision-major-camera-prior-art.md) | 👀 reviewed | A twice-iterated (2012, 2016) HelixMod fix already solves skybox/lens-flare/UI-depth for this exact D3D9 game, and confirms separate cutscene-vs-gameplay convergence handling — the strongest camera/projection prior art found so far. Factored into ENGINE-DOSSIER.md §6/§8. |
| 2026-08-25 | [Engine identity: Scimitar/Anvil + AC1 prior art](topics/2026-08-25-engine-identity-scimitar-anvil-and-ac1-prior-art.md) | ✅ incorporated | Engine confirmed as the Scimitar engine (pre-2009 Anvil), sharing real codebase lineage with Assassin's Creed (2007) — opens AC1's modding scene (EaglePatch, Anvil engine hub) as adjacent prior art. Factored into ENGINE-DOSSIER.md §2. |
| 2026-08-25 | [D3D9 + StarForce DRM profile](topics/2026-08-25-dx9-starforce-drm-profile.md) | ✅ incorporated | Renderer confirmed D3D9. DRM is StarForce, not Denuvo — retail was famously DRM-free but digital/Steam likely isn't; StarForce 5.0 has known modern-Windows compatibility issues. No vorpX profile exists for this specific game (honest gap, unlike Mad Max). **Follow-up: the modding session directly checked the installed Steam build for StarForce (driver files, service, exe strings) and found none** — see ENGINE-DOSSIER.md §4; this build appears genuinely DRM-free. |
| 2026-08-25 | [Elika / .forge asset extraction tools](topics/2026-08-25-elika-forge-asset-extraction-tools.md) | ✅ incorporated | Dedicated extraction tooling ("Elika", Turfster's .forge tool) already exists for this exact game's archive format — a positive finding, unlike Mad Max's Apex-tools gap. **⭐ 2026-09-01: re-ranked from "not urgent" to the critical path** by the modding side — the character state machine turned out to be data-driven, so the answer to the debug-camera question is inside the archives. Followed up in the 2026-09-01 topic above. |

## How to add a topic

1. New file in `topics/`, named `YYYY-MM-DD-short-slug.md`.
2. One row added to the table above, newest at the top.
3. Update the status tag here as it moves through review → incorporated/dead-end (the modding side should update this when it acts on a lead, so the index reflects reality without the research side needing to poll).
